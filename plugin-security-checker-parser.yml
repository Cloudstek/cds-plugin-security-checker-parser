name: plugin-security-checker-parser
type: action
author: Maarten de Boer <maarten@cloudstek.nl>
description: This is a plugin to parse a sensiolabs/security-checker report
parameters:
  file:
    type: string
    description: Path to security-checker output file in JSON format (e.g. generated by running vendor/bin/security-checker security:check --format=json >{{.cds.workspace}}/audit.json)
